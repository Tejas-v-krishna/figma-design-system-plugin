# Dependencies
node_modules/

# Build output — `vite.ui.config.ts` sets `emptyOutDir: true`, so this whole
# directory is wiped and rebuilt on every `npm run build`. Run the build after
# cloning to produce the bundle Figma loads.
dist/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
Thumbs.db
.idea/
*.swp

# Local tooling state
.claude/autoharness/
.claude/settings.local.json
